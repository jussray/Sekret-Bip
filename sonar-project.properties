sonar.projectKey=sekret-bip
sonar.projectName=Se'kret Bip
sonar.sources=src
sonar.tests=src
sonar.test.inclusions=**/*.test.ts,**/*.spec.ts
sonar.javascript.lcov.reportPaths=coverage/lcov.info
sonar.typescript.tsconfigPath=tsconfig.json

# Gate: API + Data – Standard
# Coverage >= 80%, 0 new bugs, 0 new vulns, 100% security hotspots reviewed
# Invariant: private teen data never reaches parent or public surfaces
