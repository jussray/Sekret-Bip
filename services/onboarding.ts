/**
 * Compatibility entry point for historical imports and documentation.
 *
 * The canonical runtime implementation lives in `src/services/onboarding.ts`.
 * Keep this file in place so older work is preserved without maintaining a
 * second active onboarding state machine.
 */
export * from '../src/services/onboarding';
