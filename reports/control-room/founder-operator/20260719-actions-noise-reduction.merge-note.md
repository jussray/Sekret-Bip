# Merge note

This branch is intentionally workflow-config-only plus documentation/report evidence.

It is designed to reduce future GitHub Actions noise by removing automatic `push`, `pull_request`, and `schedule` triggers from noisy workflows while preserving manual `workflow_dispatch` entry points.

Because this touches workflow configuration, it must not be treated as a docs-only merge. Merge separately under Founder Control Room authority.
